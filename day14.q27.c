//Write a program to print the sum of the first n odd numbers.

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int sum = n * n;
    printf("%d", sum);
    return 0;
}
